import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Lista_De_Compras',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
